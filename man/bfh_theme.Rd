% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/themes.R
\name{bfh_theme}
\alias{bfh_theme}
\title{BFH Hospital Theme for ggplot2}
\usage{
bfh_theme(base_size = 14, base_family = "Roboto Medium", colors = BFH_COLORS)
}
\arguments{
\item{base_size}{Base font size for responsive scaling (default: 14)}

\item{base_family}{Font family (default: "Roboto Medium")}

\item{colors}{Color palette (default: \link{BFH_COLORS})}
}
\value{
A ggplot2 theme object
}
\description{
Apply BFH hospital branding to SPC charts (or any ggplot2 plot).
Inspired by BBC's bbplot design philosophy: beautiful defaults with
flexible customization.
}
\details{
The theme applies:
\itemize{
\item Minimal background with subtle grid lines
\item Responsive typography based on \code{base_size}
\item Hospital brand colors
\item Publication-ready styling
}
}
\examples{
library(ggplot2)

# Basic usage
ggplot(mtcars, aes(wt, mpg)) +
  geom_point() +
  bfh_theme()

# Larger text for presentations
ggplot(mtcars, aes(wt, mpg)) +
  geom_point() +
  bfh_theme(base_size = 18)

# Custom hospital colors
my_colors <- create_color_palette(
  primary = "#003366",
  secondary = "#808080",
  accent = "#FF9900"
)
ggplot(mtcars, aes(wt, mpg)) +
  geom_point(color = my_colors$primary) +
  bfh_theme(colors = my_colors)
}
