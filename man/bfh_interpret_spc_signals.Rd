% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spc_analysis.R
\name{bfh_interpret_spc_signals}
\alias{bfh_interpret_spc_signals}
\title{Interpret SPC Signals (Anhøj Rules)}
\usage{
bfh_interpret_spc_signals(spc_stats)
}
\arguments{
\item{spc_stats}{Named list from \code{bfh_extract_spc_stats()} containing:
\itemize{
\item \code{runs_actual}: Actual longest run (consecutive points on same side of CL)
\item \code{runs_expected}: Expected maximum run length
\item \code{crossings_actual}: Actual number of crossings of centerline
\item \code{crossings_expected}: Expected minimum crossings
\item \code{outliers_actual}: Number of points outside control limits
}}
}
\value{
Character vector with Danish interpretations. Empty vector if no
signals detected and no stats provided.
}
\description{
Generates Danish standard text interpretations based on Anhøj SPC analysis.
Used as fallback when AI is not available.
}
\examples{
# Signal detected
stats <- list(runs_actual = 9, runs_expected = 7)
bfh_interpret_spc_signals(stats)

# Normal process
stats <- list(runs_actual = 5, runs_expected = 7,
              crossings_actual = 8, crossings_expected = 5)
bfh_interpret_spc_signals(stats)

}
