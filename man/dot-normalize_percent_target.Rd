% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spc_analysis.R
\name{.normalize_percent_target}
\alias{.normalize_percent_target}
\title{Normalize percent target to proportion scale}
\usage{
.normalize_percent_target(value, display, y_axis_unit)
}
\arguments{
\item{value}{Numeric target value as parsed by \code{resolve_target()}.}

\item{display}{Character display string (may be empty \code{""} for numeric input).}

\item{y_axis_unit}{Character y-axis unit (\code{"percent"}, \code{"count"}, \code{"rate"}, etc.).}
}
\value{
Numeric: \code{value / 100} when normalization applies, \code{value} otherwise.
}
\description{
Internal helper. When \code{y_axis_unit} is \code{"percent"} and the target appears
to be expressed on the 0-100 scale (either because \code{display} contains a
literal \code{"\%"} character, or because \code{value > 1} for a numeric-only input),
the value is divided by 100 so downstream comparisons work on the same
0-1 proportion scale as the centerline.
}
\details{
\code{target_display} is intentionally \strong{not} modified — user-facing text
continues to show \code{">= 90\%"} rather than \code{">= 0.90"}.

\strong{Heuristic:}
Normalize when \code{y_axis_unit == "percent"} AND
(\code{grepl("\%", display)} OR \code{value > 1})
Preserve otherwise (proportion already correct, or non-percent chart).
}
\examples{
# Percent target expressed as 0-100 (normaliseres)
BFHcharts:::.normalize_percent_target(90, ">= 90\%", "percent") # 0.90

# Numerisk input >= 1 paa percent-chart (normaliseres)
BFHcharts:::.normalize_percent_target(90, "", "percent") # 0.90

# Allerede paa proportionsskala (bevares)
BFHcharts:::.normalize_percent_target(0.9, "", "percent") # 0.9

# Ingen \% i display og value <= 1: power-user proportion-override (bevares)
BFHcharts:::.normalize_percent_target(0.9, ">= 0.9", "percent") # 0.9

# Ikke et percent-diagram: altid uaendret
BFHcharts:::.normalize_percent_target(90, ">= 90", "count") # 90

}
\keyword{internal}
