% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_label_placement.R
\name{configure_placement_cache}
\alias{configure_placement_cache}
\title{Configure all placement caches}
\usage{
configure_placement_cache(
  ttl_seconds = NULL,
  max_cache_size = NULL,
  purge_check_interval = NULL
)
}
\arguments{
\item{ttl_seconds}{TTL for cache entries in seconds (default 300 = 5 min)}

\item{max_cache_size}{Maximum number of cache entries per cache (default 100)}

\item{purge_check_interval}{Operations between purge checks (default 50)}
}
\value{
Invisible: Named list with previous configurations
}
\description{
Sets TTL and size limits for both panel height and grob height caches.
Applies same configuration to both caches for consistency.
}
\examples{
\dontrun{
# Configure for short-lived Shiny sessions (1 minute TTL)
configure_placement_cache(ttl_seconds = 60, max_cache_size = 50)

# Configure for long-running sessions (10 minutes TTL, larger cache)
configure_placement_cache(ttl_seconds = 600, max_cache_size = 200)
}
}
\seealso{
\code{\link[=configure_grob_cache]{configure_grob_cache()}}, \code{\link[=configure_panel_cache]{configure_panel_cache()}}, \code{\link[=purge_expired_cache_entries]{purge_expired_cache_entries()}}

Other placement-cache: 
\code{\link{clear_all_placement_caches}()},
\code{\link{clear_grob_height_cache}()},
\code{\link{configure_grob_cache}()},
\code{\link{configure_panel_cache}()},
\code{\link{get_placement_cache_stats}()},
\code{\link{purge_expired_cache_entries}()},
\code{\link{unlock_placement_cache_bindings}()}
}
\concept{placement-cache}
\keyword{internal}
