% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spc_compose.R
\name{bfh_analyse}
\alias{bfh_analyse}
\title{Compose a Structured SPC Analysis Object}
\usage{
bfh_analyse(x, metadata = list(), language = c("da", "en"))
}
\arguments{
\item{x}{A \code{bfh_qic_result} object from \code{bfh_qic()}.}

\item{metadata}{Optional named list. Kan indeholde:
\itemize{
\item \code{data_definition}: Description of what the data represents
\item \code{target}: Target value (numeric or operator-prefixed character)
\item \code{direction}: Optional \code{"higher_better"} / \code{"lower_better"} /
\code{"neutral"} override for direction-akse (Slice 4)
\item \code{analysis_date}: Date pinned for determinism (Phase 0.4)
\item \code{hospital}, \code{department}: passes through to context
}}

\item{language}{Character: \code{"da"} (default) or \code{"en"}. Stored in
object for downstream render-defaults; SHALL NOT cause text
resolution at this stage.}
}
\value{
Object of class \code{bfh_spc_analysis}. See ADR-005 for schema.
}
\description{
Returnerer struktureret \code{bfh_spc_analysis}-objekt med features,
conclusions (i18n-keys), caveats, suggested_actions, og
render-context. Det er primaer canonical-output for struktureret SPC-
analyse; rentekst-rendering sker via \code{bfh_render_analysis()}.
}
\details{
\strong{Key-only model:} \verb{conclusions$*_key}, \verb{caveats$*} og
\code{suggested_actions} indeholder i18n-noegler, ej resolverede tekst-
strenge. Tekst-resolution sker udelukkende i \code{bfh_render_analysis()}
via \code{texts_loader}. Dette bevarer language-neutralt
JSON-eksport-output + tillader audit-replay paa anden sprog uden
re-extraction.
}
\examples{
\dontrun{
result <- bfh_qic(data, x = month, y = value, chart_type = "i")
analysis <- bfh_analyse(result, metadata = list(target = ">= 90\%"))
print(analysis)

# Render to text:
text <- bfh_render_analysis(analysis, max_chars = 375)

# Audit-trail via JSON:
jsonlite::toJSON(as.list(analysis), auto_unbox = TRUE, Date = "ISO8601")
}

}
\seealso{
Pipeline position: second of three layers.
\itemize{
\item \code{\link{bfh_qic}} produces the \code{bfh_qic_result} input.
\item \code{\link{bfh_build_analysis_context}} is a lower-level helper
called internally (via \code{bfh_extract_spc_features()}) to build
the raw context consumed by this function.
\item \code{\link{bfh_render_analysis}} takes the returned
\code{bfh_spc_analysis} object and renders it to character output.
\item \code{\link{bfh_generate_analysis}} provides a single-step
alternative that combines analysis and rendering in one call.
\item \code{\link{bfh_generate_details}} generates the companion detail
row string (period, averages, latest values) from the same
\code{bfh_qic_result} input.
}
}
