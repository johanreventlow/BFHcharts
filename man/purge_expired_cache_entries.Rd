% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_label_placement.R
\name{purge_expired_cache_entries}
\alias{purge_expired_cache_entries}
\title{Purge expired entries from all placement caches}
\usage{
purge_expired_cache_entries(force = FALSE)
}
\arguments{
\item{force}{Logical: If TRUE, purge all entries regardless of TTL}
}
\value{
Named list with purge counts for each cache
}
\description{
Runs TTL-based purge on both panel height and grob height caches.
}
\examples{
# TTL-based purge (removes only expired entries)
purged <- purge_expired_cache_entries()
cat(
  "Purged", purged$panel_cache, "panel entries and",
  purged$grob_cache, "grob entries\n"
)

# Force purge (clears all caches)
purged <- purge_expired_cache_entries(force = TRUE)
}
\seealso{
\code{\link[=clear_all_placement_caches]{clear_all_placement_caches()}}, \code{\link[=configure_placement_cache]{configure_placement_cache()}}

Other placement-cache: 
\code{\link{clear_all_placement_caches}()},
\code{\link{clear_grob_height_cache}()},
\code{\link{configure_grob_cache}()},
\code{\link{configure_panel_cache}()},
\code{\link{configure_placement_cache}()},
\code{\link{get_placement_cache_stats}()},
\code{\link{unlock_placement_cache_bindings}()}
}
\concept{placement-cache}
