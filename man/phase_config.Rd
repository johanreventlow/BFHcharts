% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config_objects.R
\name{phase_config}
\alias{phase_config}
\title{Create Phase Configuration}
\usage{
phase_config(part_positions = NULL, freeze_position = NULL)
}
\arguments{
\item{part_positions}{Integer vector of row positions where phase changes occur (optional)}

\item{freeze_position}{Integer row position where baseline should be frozen (optional)}
}
\value{
List with class "phase_config"
}
\description{
Creates a configuration object for phase/shift handling in SPC charts.
}
\details{
This object controls phase separation and control limit freezing.

\strong{Phase Handling}:
\itemize{
\item \code{part_positions} specifies row numbers where new phases begin
\item \code{freeze_position} locks the baseline calculation up to a specific row
\item These are passed directly to qicharts2::qic()
}
}
\examples{
# No phases
phases <- phase_config()

# Phase shift at row 20
phases <- phase_config(part_positions = 20)

# Multiple phases with frozen baseline
phases <- phase_config(
  part_positions = c(15, 30, 45),
  freeze_position = 15
)
}
