% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_png.R
\name{bfh_export_png}
\alias{bfh_export_png}
\title{Export BFH QIC Chart to PNG}
\usage{
bfh_export_png(x, output, width_mm = 200, height_mm = 120, dpi = 300)
}
\arguments{
\item{x}{A \code{bfh_qic_result} object from \code{bfh_qic()}}

\item{output}{Character string specifying the output file path (e.g., "chart.png")}

\item{width_mm}{Numeric. Width of the output image in millimeters (default: 200mm)}

\item{height_mm}{Numeric. Height of the output image in millimeters (default: 120mm)}

\item{dpi}{Numeric. Dots per inch resolution for the PNG (default: 300)}
}
\value{
The input object \code{x} invisibly, enabling pipe chaining
}
\description{
Exports an SPC chart created by \code{bfh_qic()} to a PNG file with
configurable dimensions and resolution. Designed for pipe-compatible
workflows.
}
\details{
\strong{Dimension Handling:}
\itemize{
\item Dimensions are specified in millimeters (Danish/European standard)
\item Internally converted to inches for ggplot2::ggsave()
\item Default 200mm × 120mm ≈ 7.87" × 4.72" (common presentation size)
}

\strong{Resolution (DPI):}
\itemize{
\item 300 DPI (default): High quality for print and presentations
\item 150 DPI: Medium quality, smaller file size
\item 96 DPI: Screen resolution, minimal file size
}

\strong{Title Handling:}
\itemize{
\item The chart title (if present) is rendered in the PNG image
\item This differs from PDF export which strips the title for Typst template
}

\strong{Pipe Compatibility:}
\itemize{
\item Returns input object invisibly for chaining
\item Example: \code{bfh_qic(...) |> bfh_export_png("chart.png")}
}
}
\examples{
\dontrun{
library(BFHcharts)

# Create sample data
data <- data.frame(
  month = seq(as.Date("2024-01-01"), by = "month", length.out = 24),
  infections = rpois(24, lambda = 15)
)

# Create and export chart in one pipeline
bfh_qic(
  data = data,
  x = month,
  y = infections,
  chart_type = "i",
  y_axis_unit = "count",
  chart_title = "Monthly Infections"
) |>
  bfh_export_png("monthly_infections.png", width_mm = 250, height_mm = 150, dpi = 300)

# Explicit dimensions for specific use cases
result <- bfh_qic(data, month, infections, chart_type = "i")

# A4 width (210mm) for reports
bfh_export_png(result, "report_chart.png", width_mm = 210, height_mm = 140)

# PowerPoint slide (widescreen 16:9)
bfh_export_png(result, "slide_chart.png", width_mm = 254, height_mm = 143)

# Web display (lower DPI for smaller file size)
bfh_export_png(result, "web_chart.png", width_mm = 200, height_mm = 120, dpi = 96)
}
}
\seealso{
\itemize{
\item \code{\link[=bfh_qic]{bfh_qic()}} to create SPC charts
\item \code{\link[=bfh_export_pdf]{bfh_export_pdf()}} to export as PDF with Typst templates
}
}
