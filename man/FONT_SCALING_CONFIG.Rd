% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config_font_scaling.R
\docType{data}
\name{FONT_SCALING_CONFIG}
\alias{FONT_SCALING_CONFIG}
\title{Responsive Font Scaling Configuration}
\format{
Named list with scaling parameters:
\describe{
\item{divisor}{Viewport diagonal divisor (default: 3.5)}
\item{min_size}{Minimum base_size in points (default: 8)}
\item{max_size}{Maximum base_size in points (default: 48)}
}
}
\usage{
FONT_SCALING_CONFIG
}
\description{
Controls how base_size scales based on viewport dimensions (width × height).
}
\details{
\strong{Formula:}

\if{html}{\out{<div class="sourceCode">}}\preformatted{viewport_diagonal = sqrt(width_inches * height_inches)
base_size = max(min_size, min(max_size, viewport_diagonal / divisor))
}\if{html}{\out{</div>}}

\strong{Geometric Mean Approach:}
Using \verb{sqrt(width × height)} provides balanced scaling based on both
dimensions. This ensures fonts scale intuitively with overall plot size,
not just one dimension.

\strong{Parameters:}
\itemize{
\item \code{divisor}: Lower value = larger fonts (e.g., 40 → ~40\% larger than 56)
\item \code{min_size}: Minimum font size regardless of viewport
\item \code{max_size}: Maximum font size even on large screens
}

\strong{Examples (divisor = 3.5):}
\itemize{
\item 6×4 inch plot: diagonal = 4.90 → base_size = 14.0pt
\item 10×6 inch plot: diagonal = 7.75 → base_size = 22.1pt
\item 14×8 inch plot: diagonal = 10.58 → base_size = 30.2pt
}

\strong{Typical Use Cases:}
\itemize{
\item Web display: 8×5 inches (diagonal 6.02 → base_size 17.2pt)
\item Print output: 10×6 inches (diagonal 7.75 → base_size 22.1pt)
\item Presentation: 16×9 inches (diagonal 12.04 → base_size 34.4pt)
}
}
\examples{
# Default configuration
FONT_SCALING_CONFIG

# Calculate base_size for specific viewport
width <- 10  # inches
height <- 6  # inches
diagonal <- sqrt(width * height)
base_size <- max(
  FONT_SCALING_CONFIG$min_size,
  min(FONT_SCALING_CONFIG$max_size, diagonal / FONT_SCALING_CONFIG$divisor)
)
}
\seealso{
\code{\link[=calculate_base_size]{calculate_base_size()}}, \code{\link[=viewport_dims]{viewport_dims()}}

Other spc-config: 
\code{\link{calculate_base_size}()},
\code{\link{phase_config}()},
\code{\link{print.phase_config}()},
\code{\link{print.spc_plot_config}()},
\code{\link{print.viewport_dims}()},
\code{\link{spc_plot_config}()},
\code{\link{viewport_dims}()}
}
\concept{spc-config}
\keyword{datasets}
