% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spc_render.R
\name{bfh_render_analysis}
\alias{bfh_render_analysis}
\title{Render a bfh_spc_analysis Object to Character Output}
\usage{
bfh_render_analysis(analysis, max_chars = 375, texts_loader = NULL)
}
\arguments{
\item{analysis}{A \code{bfh_spc_analysis} object from \code{bfh_analyse()}.}

\item{max_chars}{Maximum characters in output. Default 375.}

\item{texts_loader}{Function that returns SPC analysis text templates.
Defaults to \code{load_spc_texts(analysis$language)}. Primarily intended
for tests/mocking.}
}
\value{
Character of length 1, \code{nchar(...) <= max_chars}.
}
\description{
Resolverer i18n-noegler i \code{analysis$conclusions}, \code{analysis$caveats}
og \code{analysis$suggested_actions} til tekst via \code{texts_loader}, sammen-
saetter base + target + action + caveats inden for \code{max_chars}-budget.
}
\details{
\code{render_context}-vaerdier (target_display, centerline_formatted,
operator_unicode, outliers_word_key) bruges verbatim som
placeholder-vaerdier. Renderer re-deriverer IKKE disse fra
features/aux for at undgaa silent display-drift.
}
\examples{
\dontrun{
result <- bfh_qic(data, x = month, y = value, chart_type = "i")
analysis <- bfh_analyse(result, metadata = list(target = ">= 90\%"))
text <- bfh_render_analysis(analysis, max_chars = 375)
}

}
