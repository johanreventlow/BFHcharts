% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_add_spc_labels.R
\name{add_spc_labels}
\alias{add_spc_labels}
\title{Add SPC labels to plot using advanced placement system}
\usage{
add_spc_labels(
  plot,
  qic_data,
  y_axis_unit = "count",
  label_size = 6,
  viewport_width = NULL,
  viewport_height = NULL,
  target_text = NULL,
  centerline_value = NULL,
  has_frys_column = FALSE,
  has_skift_column = FALSE,
  verbose = FALSE,
  debug_mode = FALSE
)
}
\arguments{
\item{plot}{ggplot object (SPC plot uden labels)}

\item{qic_data}{data.frame fra qicharts2::qic() med columns: cl, target, part}

\item{y_axis_unit}{character unit for y-akse ("count", "percent", "rate", "time", eller andet)}

\item{label_size}{numeric base font size for responsive sizing (default 6)}

\item{viewport_width}{numeric viewport width in inches (optional, for precise placement)}

\item{viewport_height}{numeric viewport height in inches (optional, for precise placement)}

\item{target_text}{character original målværdi text from user input (optional, for operator parsing)}

\item{centerline_value}{numeric centerline value from user input (optional, for BASELINE label logic)}

\item{has_frys_column}{logical TRUE if Frys column is selected (optional, for BASELINE label logic)}

\item{has_skift_column}{logical TRUE if Skift column is selected (optional, for BASELINE label logic)}

\item{verbose}{logical print placement warnings (default FALSE)}

\item{debug_mode}{logical add visual debug annotations (default FALSE)}
}
\value{
ggplot object med tilføjede labels
}
\description{
Wrapper funktion der tilføjer CL og Target labels til SPC plot
ved hjælp af NPC-baseret collision avoidance system.
}
\details{
Funktionen:
\enumerate{
\item Ekstraherer CL værdi fra seneste part i qic_data
\item Ekstraherer Target værdi fra qic_data
\item Formaterer værdier baseret på y_axis_unit
\item Opretter responsive marquee-formaterede labels
\item Kalder add_right_labels_marquee() med intelligent collision avoidance
}
}
\examples{
\dontrun{
# Basic usage
plot <- qic(x, y, data = df, chart = "i", return.data = TRUE)
plot_with_labels <- add_spc_labels(plot, plot$data, y_axis_unit = "count")

# With debug mode
plot_with_labels <- add_spc_labels(
  plot, plot$data,
  y_axis_unit = "percent",
  verbose = TRUE,
  debug_mode = TRUE
)
}

}
\seealso{
\code{\link[=add_right_labels_marquee]{add_right_labels_marquee()}}, \code{\link[=create_responsive_label]{create_responsive_label()}}, \code{\link[=format_y_value]{format_y_value()}}

Other label-placement: 
\code{\link{add_right_labels_marquee}()},
\code{\link{clear_marquee_style_cache}()},
\code{\link{create_responsive_label}()},
\code{\link{format_target_prefix}()},
\code{\link{get_right_aligned_marquee_style}()},
\code{\link{has_arrow_symbol}()},
\code{\link{sanitize_marquee_text}()}
}
\concept{label-placement}
